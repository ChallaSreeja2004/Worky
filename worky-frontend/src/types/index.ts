/**
 * src/types/index.ts
 * ==================
 * Placeholder type barrel for the Worky frontend.
 *
 * Phase 2 will add:
 *   • AuthorizationResponse  — mirrors app/auth/models.py AuthorizationResponse
 *   • ConnectorResult        — mirrors app/connectors/models.py ConnectorResult
 *   • ConnectorStatus        — mirrors ConnectorStatus enum
 *   • OutlookContext         — mirrors app/connectors/outlook/models.py
 *   • CalendarEvent          — mirrors CalendarEvent
 *   • Email                  — mirrors Email
 *
 * Types are defined here as exact mirrors of the backend Pydantic models
 * so that API responses can be safely typed at every call site.
 *
 * Do not define types that have no corresponding backend model.
 */

export {}
